##

export LS_COLORS="$(vivid generate snazzy)"
export PATH=${PATH}:${HOME}/.bin/:${HOME}/.azure/bin
export GOPATH=${HOME}/.go

alias j='z'

{{- if eq .chezmoi.os "darwin" }}
alias ls='gls --color=auto' 
{{- end }}
